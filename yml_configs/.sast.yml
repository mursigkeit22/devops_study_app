spotbugs-sast:
  
  variables:
    SAST_JAVA_VERSION: 11
    COMPILE: "false"
    MAVEN_REPO_PATH: $CI_PROJECT_DIR/backend/target/
  rules:
    - changes:
      - backend/*
  cache:
    key: backend
    paths:
      - backend/target/
  
    

semgrep-sast:
  rules:
    - changes:
      - frontend/* 
      - backend/* 
